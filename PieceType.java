package tictactoe;

public enum PieceType {
	//enum is used to define set of named constant value,for a fixed no of possible states or options for a constant state or options for a variable
   X,
   O;
	
}
